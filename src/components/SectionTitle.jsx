const SectionTitle = ({ heading }) => {
    return (
        <div>

            <h3 className="text-3xl uppercase mx-auto text-center md:w-4/12 my-8">{heading}</h3>
        </div>
    );
};

export default SectionTitle;